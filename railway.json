{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "meetwise/frontend/Dockerfile",
    "context": "meetwise/frontend"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE"
  }
}
